/**
 * Uptime ping: DISABLED (Backend now handles uptime monitoring internally).
 * Frontend pings removed to reduce frontend resources.
 * GitHub Actions + Backend cron handle all keep-alive logic.
 */
